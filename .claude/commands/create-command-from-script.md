Register an existing bash script as a new custom command.

<!-- Autocomplete stub only. The UserPromptSubmit hook runs .claude/commands/create-command-from-script.sh and exits 2 before inference is reached — this file is never read. -->
