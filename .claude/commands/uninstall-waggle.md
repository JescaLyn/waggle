Remove a dancer or the entire waggle hook. No dancer arg removes everything; scope defaults to global.

<!-- Autocomplete stub only. The UserPromptSubmit hook runs .claude/commands/uninstall-waggle.sh and exits 2 before inference is reached — this file is never read. -->
