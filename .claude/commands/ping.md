Confirms the custom command dispatcher is active.

<!-- Autocomplete stub only. The UserPromptSubmit hook runs .claude/commands/ping.sh and exits 2 before inference is reached — this file is never read. -->
