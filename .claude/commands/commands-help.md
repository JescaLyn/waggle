List all registered custom commands at the project and global level.

<!-- Autocomplete stub only. The UserPromptSubmit hook runs .claude/commands/commands-help.sh and exits 2 before inference is reached — this file is never read. -->
