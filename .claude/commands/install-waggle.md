Install a dancer hook. Dancer defaults to waggle; scope defaults to global.

<!-- Autocomplete stub only. The UserPromptSubmit hook runs .claude/commands/install-waggle.sh and exits 2 before inference is reached — this file is never read. -->
