---
name: install-waggle
description: Install a dancer hook. First arg is dancer name (default: waggle); second arg is project path (default: global).
argument-hint: [<dancer>] [<project-path>]
---

Install a dancer hook. Arguments: `$ARGUMENTS`

Run:
```bash
bash ".claude/skills/install-waggle/scripts/install.sh" $ARGUMENTS
```

Report the script output verbatim. If it exits non-zero, show the error and stop.
