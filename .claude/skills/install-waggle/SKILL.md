---
name: install-waggle
description: Install the Waggle UserPromptSubmit animation hook. No argument installs globally; pass a project path to install there instead.
argument-hint: [<project-path>]
---

Install waggle. Arguments: `$ARGUMENTS`

Run:
```bash
bash ".claude/skills/install-waggle/scripts/install.sh" "$ARGUMENTS"
```

Report the script output verbatim. If it exits non-zero, show the error and stop.
